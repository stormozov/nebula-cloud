"""
Module for storage app configuration.
"""

from django.apps import AppConfig


class StorageConfig(AppConfig):
    """Configuration for storage app."""

    name = "storage"
    verbose_name = "Файловое хранилище"
